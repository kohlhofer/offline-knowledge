[workspace]
resolver = "3"
members = ["crates/zim", "crates/core"]

[workspace.package]
edition = "2024"
version = "0.1.0"
license = "MIT"

[profile.release]
debug = "line-tables-only"
lto = "thin"
codegen-units = 4

[profile.dev.package."*"]
opt-level = 2
